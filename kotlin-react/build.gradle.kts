dependencies {
    compile(project(":kotlin-extensions"))
    compile(Dependencies.kotlinxCoroutinesCore)
    compile(Dependencies.kotlinxCoroutinesCoreCommon)
    compile(Dependencies.kotlinxCoroutinesCoreJs)
}
