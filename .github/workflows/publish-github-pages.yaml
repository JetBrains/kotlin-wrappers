# This file was generated using Kotlin DSL (.github/workflows/publish-github-pages.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Publish GitHub Pages'
on:
  workflow_dispatch:
    inputs:
      checkout-ref:
        description: 'The branch, tag or SHA to checkout. See actions/checkout ''ref''.'
        type: 'string'
        required: false
  workflow_call:
    inputs:
      checkout-ref:
        description: 'The branch, tag or SHA to checkout. See actions/checkout ''ref''.'
        type: 'string'
        required: false
  release:
    types:
    - 'published'
concurrency:
  group: 'Publish Site: ${{ github.workflow }}'
  cancel-in-progress: false
jobs:
  check_yaml_consistency:
    name: 'Check YAML consistency'
    runs-on: 'ubuntu-latest'
    steps:
    - id: 'step-0'
      name: 'Check out'
      uses: 'actions/checkout@v4'
    - id: 'step-1'
      name: 'Execute script'
      run: 'rm ''.github/workflows/publish-github-pages.yaml'' && ''.github/workflows/publish-github-pages.main.kts'''
    - id: 'step-2'
      name: 'Consistency check'
      run: 'git diff --exit-code ''.github/workflows/publish-github-pages.yaml'''
  build-site:
    name: 'Build Site'
    runs-on: 'ubuntu-latest'
    needs:
    - 'check_yaml_consistency'
    steps:
    - id: 'step-0'
      name: 'Checkout the repo'
      uses: 'actions/checkout@v4'
      with:
        ref: '${{ inputs.checkout-ref || github.ref }}'
    - id: 'step-1'
      name: 'Validate Gradle Wrapper'
      uses: 'gradle/actions/wrapper-validation@v4'
    - id: 'step-2'
      name: 'Set up JDK 21'
      uses: 'actions/setup-java@v4'
      with:
        java-version: '21'
        distribution: 'zulu'
    - id: 'step-3'
      name: 'Setup Gradle'
      uses: 'gradle/actions/setup-gradle@v4'
    - id: 'step-4'
      name: 'Build site'
      run: './gradlew :docs:dokkatooGeneratePublicationHtml'
    - id: 'step-5'
      name: 'Upload Dokka docs to Pages'
      uses: 'actions/upload-pages-artifact@v3'
      with:
        path: './docs/build/dokka/html/'
  deploy:
    name: 'Deploy Site'
    runs-on: 'ubuntu-latest'
    permissions:
      pages: 'write'
      id-token: 'write'
    needs:
    - 'build-site'
    - 'check_yaml_consistency'
    environment:
      name: 'github-pages'
      url: '${{ steps.deployment.outputs.page_url }}'
    steps:
    - id: 'deployment'
      name: 'Deploy to GitHub Pages'
      uses: 'actions/deploy-pages@v4'
