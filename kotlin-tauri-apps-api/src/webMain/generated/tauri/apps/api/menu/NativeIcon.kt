// Automatically generated - do not modify!

@file:JsModule("@tauri-apps/api/menu/iconMenuItem")

package tauri.apps.api.menu

/**
 * A native Icon to be used for the menu item
 *
 * #### Platform-specific:
 *
 * - **Windows / Linux**: Unsupported.
 */
sealed external interface NativeIcon {
    companion object {
        /** An add item template image. */
        val Add: NativeIcon

        /** Advanced preferences toolbar icon for the preferences window. */
        val Advanced: NativeIcon

        /** A Bluetooth template image. */
        val Bluetooth: NativeIcon

        /** Bookmarks image suitable for a template. */
        val Bookmarks: NativeIcon

        /** A caution image. */
        val Caution: NativeIcon

        /** A color panel toolbar icon. */
        val ColorPanel: NativeIcon

        /** A column view mode template image. */
        val ColumnView: NativeIcon

        /** A computer icon. */
        val Computer: NativeIcon

        /** An enter full-screen mode template image. */
        val EnterFullScreen: NativeIcon

        /** Permissions for all users. */
        val Everyone: NativeIcon

        /** An exit full-screen mode template image. */
        val ExitFullScreen: NativeIcon

        /** A cover flow view mode template image. */
        val FlowView: NativeIcon

        /** A folder image. */
        val Folder: NativeIcon

        /** A burnable folder icon. */
        val FolderBurnable: NativeIcon

        /** A smart folder icon. */
        val FolderSmart: NativeIcon

        /** A link template image. */
        val FollowLinkFreestanding: NativeIcon

        /** A font panel toolbar icon. */
        val FontPanel: NativeIcon

        /** A `go back` template image. */
        val GoLeft: NativeIcon

        /** A `go forward` template image. */
        val GoRight: NativeIcon

        /** Home image suitable for a template. */
        val Home: NativeIcon

        /** An iChat Theater template image. */
        val IChatTheater: NativeIcon

        /** An icon view mode template image. */
        val IconView: NativeIcon

        /** An information toolbar icon. */
        val Info: NativeIcon

        /** A template image used to denote invalid data. */
        val InvalidDataFreestanding: NativeIcon

        /** A generic left-facing triangle template image. */
        val LeftFacingTriangle: NativeIcon

        /** A list view mode template image. */
        val ListView: NativeIcon

        /** A locked padlock template image. */
        val LockLocked: NativeIcon

        /** An unlocked padlock template image. */
        val LockUnlocked: NativeIcon

        /** A horizontal dash, for use in menus. */
        val MenuMixedState: NativeIcon

        /** A check mark template image, for use in menus. */
        val MenuOnState: NativeIcon

        /** A MobileMe icon. */
        val MobileMe: NativeIcon

        /** A drag image for multiple items. */
        val MultipleDocuments: NativeIcon

        /** A network icon. */
        val Network: NativeIcon

        /** A path button template image. */
        val Path: NativeIcon

        /** General preferences toolbar icon for the preferences window. */
        val PreferencesGeneral: NativeIcon

        /** A Quick Look template image. */
        val QuickLook: NativeIcon

        /** A refresh template image. */
        val RefreshFreestanding: NativeIcon

        /** A refresh template image. */
        val Refresh: NativeIcon

        /** A remove item template image. */
        val Remove: NativeIcon

        /** A reveal contents template image. */
        val RevealFreestanding: NativeIcon

        /** A generic right-facing triangle template image. */
        val RightFacingTriangle: NativeIcon

        /** A share view template image. */
        val Share: NativeIcon

        /** A slideshow template image. */
        val Slideshow: NativeIcon

        /** A badge for a `smart` item. */
        val SmartBadge: NativeIcon

        /** Small green indicator, similar to iChat's available image. */
        val StatusAvailable: NativeIcon

        /** Small clear indicator. */
        val StatusNone: NativeIcon

        /** Small yellow indicator, similar to iChat's idle image. */
        val StatusPartiallyAvailable: NativeIcon

        /** Small red indicator, similar to iChat's unavailable image. */
        val StatusUnavailable: NativeIcon

        /** A stop progress template image. */
        val StopProgressFreestanding: NativeIcon

        /** A stop progress button template image. */
        val StopProgress: NativeIcon

        /** An image of the empty trash can. */
        val TrashEmpty: NativeIcon

        /** An image of the full trash can. */
        val TrashFull: NativeIcon

        /** Permissions for a single user. */
        val User: NativeIcon

        /** User account toolbar icon for the preferences window. */
        val UserAccounts: NativeIcon

        /** Permissions for a group of users. */
        val UserGroup: NativeIcon

        /** Permissions for guests. */
        val UserGuest: NativeIcon
    }
}
